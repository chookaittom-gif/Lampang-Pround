# Cloudflare Workers Static Assets headers
# /assets/* คือไฟล์ Vite build ที่มี content hash ในชื่อ — เนื้อหาเปลี่ยนเมื่อชื่อเปลี่ยน
# จึง cache ได้ปลอดภัยตลอดอายุ (HTML ไม่อยู่ในกฎนี้ คง max-age=0 เพื่อรับไฟล์ชุดใหม่เสมอ)
/assets/*
  Cache-Control: public, max-age=31536000, immutable
